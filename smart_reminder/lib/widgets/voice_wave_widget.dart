import 'package:flutter/material.dart';

class VoiceWaveWidget extends StatelessWidget {
  const VoiceWaveWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Wave');
  }
}